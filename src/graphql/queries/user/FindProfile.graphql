query FindProfile {
	findProfile {
		id
		username
		displayName
		bio
		email
		avatar
		isTotpEnabled
    isVerified
		notificationsSettings {
			siteNotifications
			telegramNotifications
		}
	}
}
